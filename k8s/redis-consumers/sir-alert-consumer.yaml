apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-consumer-sir-alert
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-consumer
      feed: sir
      type: alert
  template:
    metadata:
      labels:
        app: redis-consumer
        feed: sir
        type: alert
    spec:
      containers:
        - name: consumer
          image: johnmccormick7/mta-gtfs-pipeline:gtfs-redis-consumer-latest
          env:
            - name: KAFKA_TOPICS
              value: "feed-sir-alert"
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: brokers
            - name: REDIS_ADDR
              valueFrom:
                configMapKeyRef:
                  name: redis-config
                  key: redis_addr
